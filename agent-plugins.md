# Agent Plugins in the Marketplace

Marketplace entries may declare `format` as `secagent` or `agent`.

An `agent` entry identifies an [Agent Plugins 1.0.0](https://agent-plugins.org/) package. Its release ZIP must contain `plugin.json` at the package root and may contain `skills/` and `mcp.json`.
